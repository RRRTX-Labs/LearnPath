# Content

Canonical curriculum is TypeScript modules in `src/lib/content/`, validated with Zod.

This folder exists so GitHub collaborators have a documented place for roadmaps, skills, resources, projects, and challenges. Until a JSON export lands, edit the TypeScript catalog and run:

```bash
npm run content:validate
```

Do not vendor roadmap.sh files here.
