import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export function formatDuration(minutes: number): string {
  if (minutes < 60) return `${minutes} min`;
  const h = Math.floor(minutes / 60);
  const m = minutes % 60;
  return m ? `${h}h ${m}m` : `${h}h`;
}

export function slugify(value: string): string {
  return value
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/(^-|-$)/g, "");
}

export const APP_NAME = "LearnPath";
export const APP_TAGLINE = "Learn anything. Build everything.";
export const APP_DESCRIPTION =
  "A free, open-source, roadmap-first learning platform. Curated free resources, in-browser practice, challenges, and projects — with progress that follows you.";
